# Keeps the public/ directory present in fresh clones. Next.js standalone builds
# (and the Dockerfile's `COPY --from=build /app/public ./public`) expect it to exist
# even when there are no static assets to serve.
